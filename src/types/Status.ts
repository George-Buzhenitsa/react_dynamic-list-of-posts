export enum Status {
  IDLE = 'idle',
  Loading = 'loading',
  Success = 'success',
  Error = 'error',
}

export enum FieldsErrorStatus {
  IDLE = 'idle',
  name = 'name',
  email = 'email',
  comment = 'comment',
}
